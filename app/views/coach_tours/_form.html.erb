<%= form_for @coach_tour, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :coachtour_id, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :coachtour_id, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  coach_tours_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
