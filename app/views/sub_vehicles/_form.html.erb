<%= simple_form_for(@sub_vehicle) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :subvehicle_id %>
    <%= f.association :vehicle %>
    <%= f.association :coach_tour %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
