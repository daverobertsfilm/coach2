<%- model_class = SubVehicle -%>
<div class="page-header">
  <h1><%=t '.title', :default => SubVehicle %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:subvehicle_id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @sub_vehicles.each do |sub_vehicle| %>
      <tr>
        <td><%= sub_vehicle.id %></td>
        <td><%= sub_vehicle.subvehicle_id %></td>
        <td><%=l sub_vehicle.created_at, format: :long %></td>
        <td>
          <%= link_to sub_vehicle_path(sub_vehicle), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to sub_vehicle_path(sub_vehicle), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_sub_vehicle_path,
            :class => 'btn btn-primary' %>
